apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: fullstack
spec:
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30188
  selector:
    app: api-gateway
  type: NodePort
